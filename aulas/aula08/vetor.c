#include <stdio.h>

int main(void)
{
    float capital = 30000
    scanf("%f", %capital);

    for (int i = 0; i < 12, i++)
    {
        capital += 1500;
        capital *= 1,1;
        printf("%f", capital);
    }

} 